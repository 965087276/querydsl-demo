spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/test?characterEncoding=utf8
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy:
#          驼峰命名到数据库里变成下划线命名
          org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy